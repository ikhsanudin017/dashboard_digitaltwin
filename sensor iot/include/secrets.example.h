#ifndef SENSOR_IOT_SECRETS_H
#define SENSOR_IOT_SECRETS_H

// Copy this file to `secrets.h` (same folder), then fill with real values.
// File `secrets.h` is ignored by git and should never be committed.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

#define IOT_HUB_NAME "your-iot-hub-name"
#define IOT_DEVICE_ID "ESP32_ENERGY_MONITOR_001"
#define IOT_DEVICE_KEY "YOUR_DEVICE_PRIMARY_KEY"

#endif
